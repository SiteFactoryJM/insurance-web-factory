# Clear Human 검증 안내

검증일: 2026-09-17

이 문서는 현재 소스의 검증 범위와 실제 실행 결과만 기록합니다. 배포·심의·실기기 연락 확인은 코드 검사와 구분합니다.

## 자동 검사 범위

- `npm run check`: 사이트 레지스트리 생성, 제작 번들 생성, 설정 검증, TypeScript, 단위 테스트
- `npm run test:e2e`: 고객 페이지와 제작 화면, 320·360·390·768·1440px, 200% 글자 확대, 접근성, 30개 구성·색상 조합
- 이름 카드의 6개 팔레트 보조 글자 대비 4.5:1 이상
- 상담분야 cards/split의 3·4·5·6개 항목 대칭 배치와 긴 한국어 무잘림
- 모바일 하단 연락 버튼, 프로필 이미지 대체 텍스트, 직접 전화·오픈채팅, 고객 입력 미저장
- `draft/noindex`와 `published/index` 구분, 이전 `/api/consultations`의 HTTP 410

## 이번 실행 결과

- `npm run check`: 성공. 사이트 1개, 도메인 0개, 설정 오류·경고 0개, 단위 테스트 203개 통과.
- `npx playwright test --config tmp/review-playwright.config.mjs`: 성공. 기존 8787 서버와 분리된 새 8798 빌드에서 27개 시나리오 통과(2분 18초).
- 5개 화면 폭과 200% 확대에서 수평 오버플로가 없었고, 5개 구성×6개 팔레트와 WCAG A/AA axe 검사가 통과했습니다.
- cards/split 각각 3·4·5·6개 항목의 행 좌우 여백과 내부 오버플로를 검사했고 모두 통과했습니다.
- 변경 후 element 캡처는 `artifacts/alignment-after/`에 저장했습니다.

## 별도 확인

- 실제 담당자 기기의 전화 앱과 카카오톡 로그인·오픈채팅방 활성 상태
- 실제 도메인 소유·DNS·게시 승인
- 광고심의 대상 여부와 소속 조직 승인
- 배포된 주소에서 새 CSS 마커와 화면이 제공되는지 확인

위 항목은 자동 검사 통과만으로 완료되었다고 보지 않습니다.
